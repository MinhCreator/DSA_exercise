int Min(int a[], int n)
{
    int i, min;
    min = a[0];
    for(i = 2; i < n; i++)
        if(a[i]< min)
            min = a[i]
    return min;
}
if the target have first index in array this code block has complexity that : O(1)
but it can have worst cased when was target find in arr[n]: O(n)
